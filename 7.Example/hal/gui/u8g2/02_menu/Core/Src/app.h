

#include "stdbool.h"
#include "math.h"
#include "utils.h"

#include "easing/easing.h"

#define ARRAR_SIZE(arr) (sizeof(arr) / sizeof(arr[0]))

#define min(a, b) ((a < b) ? (a) : (b))

////////////////////////////////////////////////////////////////////////

#define SPEED 8
